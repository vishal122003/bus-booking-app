import { FilterSeatsPipe } from './filter-seats.pipe';

describe('FilterSeatsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSeatsPipe();
    expect(pipe).toBeTruthy();
  });
});
